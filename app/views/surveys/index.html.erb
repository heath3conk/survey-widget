<div class="row">
  <h1 class="col s12 m12 l12 page-title">Active Surveys</h1>
  <h2 class="explanation">Click on a survey's title to see and/or edit its questions</h2>
</div>

  <%= render partial: 'surveys', locals: { surveys: @surveys } %>

<% if current_user.role == "Course Author" %>
  <div class="center-align buttons">
    <%= link_to "ADD NEW SURVEY", new_survey_path, class:"waves-effect waves-light btn-large extra-large-button"%>
  </div>
<% end %>

<!-- This modal isn't working. Hoping to come back to it before I'm done even if just to make it a div that toggles visibility. Need to get questions working first for MVP. -->
<div id="survey-modal" class="modal">
  <div class="modal-content">
    <h4>Please enter the title of your new survey?</h4>
    <%= form_for :survey, remote: true, url: surveys_path do |f| %>
      <%= f.text_field :title, id:"new-title"%>
      <div class="modal-footer">
        <a href="surveys" class="modal-action modal-close waves-effect waves-light btn-large extra-large-button">Create Survey</a>
        <!-- I wonder if the problem is that this is a link and not a button? -->
      </div>
    <% end %>
  </div>
</div>